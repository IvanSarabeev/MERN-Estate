{
  "name": "mern-estate",
  "version": "1.0.0",
  "description": "Full-stack MERN application.",
  "scripts": {
    "build": "npm install && npm install --prefix client && npm run build --prefix client && npm run build --prefix server",
    "start": "npm run build && npm run dev --prefix server",
    "dev": "npm run dev --prefix server"
  },
  "keywords": [],
  "author": "Ivan Sarabeev",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {}
}
